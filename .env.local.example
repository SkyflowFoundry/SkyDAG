# SkyDAG Configuration Template
# Copy this file to .env.local and edit values for your environment

# REQUIRED: Platform selection (gcp, aws, or azure)
SKYDAG_PLATFORM=gcp

# REQUIRED: Source location to read files from (bucket auto-created with unique suffix)
# GCP/AWS format: "bucket/prefix" 
# Azure format: "container/prefix"
# Note: Your GCP project ID will be added as suffix (e.g., my-source-bucket-solutionseng)
SKYDAG_SOURCE=my-source-bucket/input-files

# REQUIRED: Destination location to write processed files (bucket auto-created with unique suffix)
# Same format as source
# Note: Your GCP project ID will be added as suffix (e.g., my-dest-bucket-solutionseng)
SKYDAG_DEST=my-dest-bucket/output-files

# REQUIRED: Skyflow API endpoints
# To get these values:
# 1. Log into your Skyflow environment
# 2. Create a new Vault from Template using the "Detect Template"
# 3. Click the settings icon (gear) in the left navigation
# 4. Copy the Vault URL and Vault ID from the settings page
# 5. Use the Vault URL as your base URL, add the detect endpoint path
SKYFLOW_START_URL=https://your-vault.vault.skyflowapis.com/v1/detect/deidentify/file
SKYFLOW_POLL_URL_TEMPLATE=https://your-vault.vault.skyflowapis.com/v1/detect/runs/{run_id}?vault_id=your-vault-id

# Example: If your Vault URL is https://ebfc9bee4242.vault.skyflowapis.com
#          and your Vault ID is c509d631695d40c98df01c021d2d5ab2, then:
#          SKYFLOW_START_URL=https://ebfc9bee4242.vault.skyflowapis.com/v1/detect/deidentify/file
#          SKYFLOW_POLL_URL_TEMPLATE=https://ebfc9bee4242.vault.skyflowapis.com/v1/detect/runs/{run_id}?vault_id=c509d631695d40c98df01c021d2d5ab2

# REQUIRED: Authorization header (include "Bearer " prefix)
# To get an auth token:
# Option 1 (Quick): Click your profile icon (circle with initials) in the top-right
#                   and copy the auth token from the dropdown menu
# Option 2 (Recommended): Create a dedicated service account for API access and 
#                   input a Service Account API Key or JWT generated from a Service Account
SKYFLOW_AUTH_HEADER='Bearer your-auth-token-here'

# OPTIONAL: Polling configuration (seconds)
SKYDAG_POLL_MAX_WAIT=1800
SKYDAG_POLL_INITIAL=2.0
SKYDAG_POLL_BACKOFF=1.7
SKYDAG_POLL_MAX_INTERVAL=20.0

# ==========================================
# REMOTE DEPLOYMENT CONFIGURATION
# ==========================================

# REQUIRED: Deployment platform (gcp, aws, or azure)
DEPLOY_PLATFORM=gcp

# OPTIONAL: Deployment environment name (dev, staging, prod)
DEPLOY_ENVIRONMENT=dev

# REQUIRED: Bucket/container for DAG files (GCP project ID added as suffix automatically)
DEPLOY_DAG_BUCKET=my-airflow-dags-bucket

# OPTIONAL: Prefix for DAG files in bucket (default: "dags")
DEPLOY_DAG_PREFIX=dags

# ==========================================
# GCP COMPOSER DEPLOYMENT
# ==========================================

# REQUIRED: GCP project and region settings
# Find these in your Google Cloud Console
DEPLOY_GCP_PROJECT=my-gcp-project
DEPLOY_GCP_REGION=us-central1
DEPLOY_COMPOSER_ENVIRONMENT=my-composer-env

# REQUIRED: Authentication for GCP services
# Method 1: Use gcloud CLI (recommended for development)
#           Run: gcloud auth login && gcloud config set project YOUR_PROJECT_ID
# Method 2: Service account JSON file path
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# Method 3: Inline JSON credentials (for deployment automation)
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}

# ==========================================
# AWS MWAA DEPLOYMENT  
# ==========================================
# Uncomment and configure if using AWS MWAA

# DEPLOY_AWS_REGION=us-east-1
# DEPLOY_MWAA_ENVIRONMENT=my-mwaa-env

# Optional: AWS credentials (or use AWS CLI default profile)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ==========================================
# AZURE DATA FACTORY DEPLOYMENT
# ==========================================
# Uncomment and configure if using Azure Data Factory + Airflow

# DEPLOY_AZURE_SUBSCRIPTION_ID=your-subscription-id
# DEPLOY_AZURE_RESOURCE_GROUP=my-resource-group
# DEPLOY_AZURE_DATA_FACTORY=my-data-factory
# DEPLOY_AZURE_STORAGE_ACCOUNT=mydagsstorageaccount

# Optional: Service Principal credentials (or use Azure CLI default)
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret